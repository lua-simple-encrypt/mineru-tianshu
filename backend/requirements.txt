# MinerU-Server Backend Unified Requirements
# ============================================================================
# Core Frameworks (Handled in Dockerfile for stability)
# ============================================================================
# vllm>=0.15.1 (Will install PyTorch automatically)
# paddlepaddle-gpu==3.3.0
# torch, torchvision, torchaudio (Managed by vLLM)

# ============================================================================
# OCR & Pipeline Engines
# ============================================================================
mineru[core]>=2.7.6
doclayout-yolo>=0.0.2
ftfy>=6.1.0

# PaddleOCR - Auto language detection
paddleocr>=2.8.0

# Transformers & Tokenizers
# Note: Specific versions are handled in Dockerfile to avoid conflict
transformers>=4.40.0
tokenizers>=0.22.0

# ============================================================================
# Image Processing
# ============================================================================
PyMuPDF>=1.24.0
Pillow>=11.0.0
img2pdf>=0.5.0
pikepdf>=9.0.0

# Image Augmentation (Strict constraint for MinerU compatibility)
albumentations>=1.3.1,<1.4.0
albucore>=0.0.13,<0.0.17
kiwisolver>=1.4.5
matplotlib>=3.9.0
contourpy>=1.3.0

# ============================================================================
# Audio & Video
# ============================================================================
funasr>=1.2.7
ffmpeg-python>=0.2.0
soundfile>=0.12.0
opencv-python>=4.10.0
imagehash>=4.3.0
numpy>=1.26.0,<2.0.0

# ============================================================================
# Bioinformatics
# ============================================================================
biopython>=1.80

# ============================================================================
# Watermark Removal & AI Models
# ============================================================================
ultralytics>=8.3.0
simple-lama-inpainting>=0.1.0
huggingface-hub>=0.23.2
modelscope>=1.18.0
safetensors>=0.7.0

# ============================================================================
# Web Framework & API
# ============================================================================
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart>=0.0.9
litserve==0.2.16
aiohttp==3.11.11

# ============================================================================
# Auth & Utils
# ============================================================================
pyjwt>=2.9.0
authlib>=1.3.0
python3-saml>=1.16.0
pydantic[email]==2.10.5
pydantic-core==2.27.2
email-validator>=2.2.0
loguru>=0.7.0
python-dotenv>=1.0.0
markitdown[all]>=0.1.3
minio>=7.2.0
redis>=5.0.0

# ============================================================================
# MCP Protocol
# ============================================================================
mcp==1.1.2
sse-starlette==2.2.1
jsonschema>=4.20.0
jsonschema-specifications>=2023.12.1

# ============================================================================
# Build Tools (Python 3.12)
# ============================================================================
typing-extensions>=4.12.0
setuptools>=75.0.0
lxml>=5.3.0
